{
    // have x := 45;
    // x = x * 2;
    // info x; 
    {
      //evaluate the 'else' expression if the condition is false 
      // if (true) info "good"; else info "bad"; // expected: good
      // if (false) info "bad"; else info "good"; // expected: good

      // if (false) {
      //    info "bad";
      // } else {
      //    info "good";
      // }

      // have a := false;
      // if (a = true) info a; // expected: true
    } 
    {
        // Block body
        // while (true) {
        //     info "Hello There!";
        // }

        // Single expression body
        // have d := 0;
        // while (d < 10) info d = d + 1;

        // Statement body
        // while (false) if (true) 1; else 2;
        // while (false) while (true) 1;
    }
    {
      // for (have i := 1; i < 3;) {
      //    info i;
      // }
    }
    {
      switch (3) {
          case 1:
            info "one";
          case 2:
            info "two";
          case 3:
            info "three";
        default:
          info "default";
      }
      info "after";
    }
}

