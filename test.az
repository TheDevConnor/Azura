class Math-as {
    add(a, b) {
        return a + b;
    }

    sub(a, b) {
        return a - b;
    }
}

class Math < Math-as {
    mult(a, b) {
        return a * b;
    }

    div(a, b) {
        return a / b;
    }
}

have operations := Math();

info operations.add(1, 2); // expect 3