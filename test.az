class Math {
    add(a, b) {
        return a + b;
    }

    sub(a, b) {
        return a - b;
    }

    mult(a, b) {
        return a * b;
    }
}

have math := Math();

info math.add(1, 3);  // Expect 4
info math.sub(3, 1);  // Expect 2
info math.mult(3, 3); // Expect 9