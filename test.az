{
    have x := sin(30); // By default, the argument is in degrees
    info x; // Expect 0.5
}
{
    have x := cos(30, true); // When the second argument is true, the argument is in radians
    info x; // Expect 0.8660254037844386
}
{
    have x := tan(30); // By default, the argument is in degrees
    info x;
}