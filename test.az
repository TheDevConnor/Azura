{
    have x := sin(30); // By default, the argument is in radians
    info x; // Expect -0.9880316240928618 but we will get -0.988032
}
{
    have x := cos(30); // By default, the argument is in radians
    info x; // Expect 0.15425144988758405 but we will get 0.154251
}
{
    have x := tan(30); // By default, the argument is in radians
    info x; // Expect -6.405331196646276 but we will get -6.40533
}