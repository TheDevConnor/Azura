have x := [1, 2, 3, 4, 5];
info x; // [1, 2] TODO: should be [1, 2] but we get 2 instead of [1, 2]
        // I think this is because we are not using the correct type for the info function
        // Or it could be how we are handling arrays in the parser