#ifndef azura_strings_h
#define azura_strings_h

#include "common.h"
#include "value.h"

Value toUpperCaseNative(int argCount, Value* args);
Value toLowerCaseNative(int argCount, Value* args);

#endif